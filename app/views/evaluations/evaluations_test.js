'use strict';

describe('app.evaluations module', function() {

  beforeEach(module('app.evaluations'));

  describe('evaluations controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var evaluationsController = $controller('EvaluationsController');
      expect(evaluationsController).toBeDefined();
    }));

  });
});
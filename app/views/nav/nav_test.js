'use strict';

describe('app.nav module', function() {

  beforeEach(module('app.nav'));

  describe('nav controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var navCtrl = $controller('navController');
      expect(navCtrl).toBeDefined();
    }));

  });
});
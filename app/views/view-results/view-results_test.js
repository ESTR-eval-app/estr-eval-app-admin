'use strict';

describe('app.new-evaluation module', function () {

  beforeEach(module('app.new-evaluation'));

  describe('new-evaluation controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var newEvaluationController = $controller('NewEvaluationController');
      expect(newEvaluationController).toBeDefined();
    }));

  });
});
{
  "name": "eval-n-admin",
  "private": true,
  "version": "0.0.0",
  "description": "Admin web application for Eval n",
  "repository": "https://github.com/ESTR-eval-app/estr-eval-app-admin",
  "license": "MIT",
  "devDependencies": {
    "bower": "^1.3.1",
    "http-server": "^0.6.1"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
